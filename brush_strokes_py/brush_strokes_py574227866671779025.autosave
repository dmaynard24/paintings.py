

def setup():  
  background(255)

  # source image. swap this out.
  img = loadImage("C:\\Users\\dmaynard\\Google Drive\\Pictures\\smiley-dog-cropped.jpg")
  size(240, 130) # set size to the size of your source image
  
def draw():
  if mousePressed:
    fill(0)
  else:
    fill(255)
  ellipse(mouseX, mouseY, 80, 80)
